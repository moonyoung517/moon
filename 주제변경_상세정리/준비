0.발표 목차 : KDD Process에 따라 진행
    1. Raw Data(Data Platform) 데이터 정밀지도
    2. Target data : table_map, column_map, column_summary_map, table_summary_map(대상 빅데이터 개요 및 특성 기재종류, 크기, 기간, 유형 등))
    3. data transformation : 데이터 전처리
        3-1 (+) : 컬럼 유형 변환 : 수치형-> 범주형, 범주형-> 속성 일반화(Attribute Generalization), 파생 변수(Attribute Creation) 정의
        3-2 (-) : 불필요한 차원(속성) 삭제, 지도학습용 데이터의 차원 축소, 비지도학습용 데이터의 차원 축소, Data Cleaning (Errors), 
        3-3 (x) : 데이터 결합 , 데이터 결합용 공통 식별자 컬럼정하기, 이종 합성데이터 쌍의 합성(익명) 결합 활용
        3-4(/) : Subspace Focusing,  

    4. Data mining : 데이터마이닝 알고리즘 적용
        빅데이터 분석 결과 평가
        - 적용한 전처리 사항 효과 검증
        - 선택한 함수 효과 검증
        - 선택한 알고리듬 검증
        - 사용한 임계값 효과 검증
        분석 모델/알고리듬 수행 환경 (매개변수 값 등 )
        - 주요 데이터 전처리 작업들의 효과 검증
         분석 목적 정합성 평가 (분석 정확성, 가설 검증 비교)
        ❖ 분석 결과 비교 평가(2~3번 수치 다르게 하여 비교)

    5. 추가 분석 사항 도출 -> 도메인 주요 지식 발굴
    학교 마스터 테이블에 대해서도 같은 결과 확인

1. 서론(발표 스토리)

저번 발표에서 테이블맵 클러스터링: 전체 데이터맵을 클러스터링으로 분석해 본 결과, '학교'라는 키워드를 중심으로 수많은 테이블이 군집을 이루고 있는 것을 확인했고(수동)--> AI 선도학교 라는 주제로 도메인 내 BI를 찾아보려함

But AI 도입은 몇년 되지 않아 의미있는 주제를 찾기 힘들 수 있음(Feedback)
다른 주제를 찾다가 다시 도메인에 specific한 결과가 나올수도 있을 것 같아 datamap을 통해 지능화와 자동화의 취지에 맞게 학교라는 키워드를 자동으로 뽑아낼 수 있을까?를 보려함
데이테맵 데이터를 일차적으로 클러스터링을 통해 분류별 테이블을 확인 --> 데이터맵 상세 분석을 진행하여 주제 자동 도출


2. 진행 과정

메타테이블 결합
교육 카테고리의 공통 도메인성 확인
학교명/지역/학교급 축 발견


    1. 데이터맵의 PK-FK 연결 가능성 확인을 위하여 datamap 메타테이블들을 결합해서 어떤 카테고리가 공통 도메인을 많이 공유하는지 확인하여 분석 대상 선택

    1-1  datamap 메타테이블 4개(table_map, column_map, column_summary_map, table_summary_map)를 table_id, col_id로 결합해  9,840행 x 62컬럼의 메타 분석 테이블 생성 (data transformation: x)

    [PK/FK 결합 체계]

    table_map (999×18)         ← PK: table_id
        ↑ FK: table_id
    column_map (9,840×33)      ← 중심 테이블
        ↑ FK: col_id
    column_summary_map (8,359×164)  ← PK: col_id (도메인 유사도)
        ↑ FK: table_id
    table_summary_map (995×113)     ← PK: table_id (테이블 응집도)

    결합 결과: 9,840행 × 62컬럼
    단일 파일 대비 +29 컬럼 추가

    ->01_joined_profile

    1-2 파생변수 만들어서 이 신규 테이블을 통해 어떤 컬럼명이 얼마나 많은 테이블에 반복되고, 다른 테이블과 얼마나 비슷한 도메인을 이루는가를 확인하려 함(연결 가능성 확인)(data transformation : +, -)

    [파생변수별 생성 기준]
    수치형 변환 자체가 목적이 아니라, 조인 가능성과 도메인 공유 구조를 가장 설명력 있게 표현하기 위한 목적형 설계(수동으로 선정)

    1) 버킷화(수치형 → 범주형)
    - 대상: `pk_ratio`, `num_cat_flag`, `rec_cnt`, `column_cnt`, `col_table_cnt`, `avg_dom_col`, `avg_val_col`
    - 이유: 조인 핵심 축(PK강도, 테이블규모, 도메인공유, 도메인/값 유사도)을 해석 가능한 범주로 변환해 AOI 및 규칙 마이닝 입력으로 사용하기 위함
    - 예시: `rec_cnt`는 `소/중/대/초대`, `col_table_cnt`는 `단독/소공유/중공유/대공유`로 구간화


    pk_bucket
    원본: pk_ratio
    규칙: 00.3, 0.30.7, 0.7~0.9, 0.9+
    의미: 식별키 강도(저PK~최고PK)

    col_type
    원본: num_cat_flag
    규칙: 0→수치형, 1→범주형
    의미: 수치/범주 메타 구분

    rec_bucket
    원본: rec_cnt
    규칙: ≤100, 1011k, 1k10k, 10k+
    의미: 테이블 규모

    col_cnt_bucket
    원본: column_cnt
    규칙: ≤5, 610, 1120, 21+
    의미: 테이블 폭(컬럼 수) 구조

    domain_spread
    원본: col_table_cnt
    규칙: 1(단독), 25, 620, 21+
    의미: 컬럼 도메인의 공유 범위(핵심 결합축)

    dom_sim_bucket
    원본: avg_dom_col
    규칙: 00.1, 0.10.3, 0.3~0.6, 0.6+
    의미: 도메인 유사도 강도

    val_sim_bucket
    원본: avg_val_col
    규칙: 00.1, 0.10.3, 0.3~0.6, 0.6+
    의미: 값 유사도 강도


    실무 해석 단위와 맞춤: “저/중/고”, “소/중/대”로 즉시 설명 가능
    마이닝 적합성: FP-Growth/연관규칙은 범주 토큰이 필요
    극단값 완충: 연속값 그대로보다 구간화가 노이즈에 강함


    (테이블 규모는 조인 키 그 자체는 아니지만, 조인 품질(정확성), 안전성(폭증/결측), 실무성(비용/성능)을 동시에 설명하는 핵심 맥락 변수라서 조인 핵심축으로 본 겁니다.)

    col_table_cnt가 담는 정보
    의미: 해당 컬럼명(또는 컬럼 도메인)이 몇 개 테이블에 나타나는지를 세는 카운트입니다.

    왜 “공유 범위(핵심 결합축)”를 알 수 있나:

    같은 컬럼이 많은 테이블에 반복될수록, 조인 후보 축으로 재사용될 가능성이 커집니다.
    이를 단독 / 소공유(2-5) / 중공유(6-20) / 대공유(21+)로 바꿔서 해석합니다.
    예시로 코드명, 연번 같은 항목은 col_table_cnt가 700+로 매우 높아 공유축 성격이 강합니다.

    avg_dom_col이 담는 정보
    의미: **컬럼 도메인 유사도의 평균 강도(0~1 계열)**입니다.
    근거: 같은 파일에 num_dom_eqn_col, num_dom_inter_col, avg_dom_eqn_col 같은 보조 지표가 같이 존재합니다. 즉 “도메인 동일/교집합/포함” 관계를 집계한 평균형 지표로 보는 게 맞습니다.
    이를 dom_sim_bucket으로 구간화합니다.

    왜 유사도 강도를 알 수 있나:

    값이 클수록 다른 컬럼들과 도메인 구조가 더 유사하다는 뜻입니다.
    코드에서도 0.1, 0.3, 0.6 경계로 낮음/중간/높음/최고 유사로 나눕니다.

    avg_val_col이 담는 정보
    의미: **실제 값 분포/값 겹침 관점의 평균 유사도(0~1 계열)**입니다.
    근거: num_val_eqn_col, num_val_inter_col, avg_val_eqn_col 계열 필드가 함께 제공됩니다.
    BI-6는 이를 val_sim_bucket으로 구간화합니다.
    사용 코드: bi6_datamap_join_analysis.py:133
    왜 유사도 강도를 알 수 있나:

    값이 클수록 실제 값 레벨에서 겹치거나 비슷한 컬럼이라는 뜻입니다.
    도메인 유사(avg_dom_col)는 높은데 값 유사(avg_val_col)는 낮은 사례도 존재해, “이름/역할은 비슷하지만 값은 다름” 같은 해석이 가능합니다.

    즉 여기서 본 것은 “실제 학교 데이터의 값”이 아니라, “어떤 컬럼명이 얼마나 많은 테이블에 반복되고, 다른 테이블과 얼마나 비슷한 도메인을 이루는가”였습니다.


    2) 유지(수치형 원값 유지)
    - 대상: `distinct_cnt`, `same_ratio`, `categorical_cnt`, `numerical_cnt`, `num_dom_eqn_col`, `num_dom_inter_col`, `num_val_eqn_col`, `num_val_inter_col`, `inner_tbl_cnt`, `outer_tbl_cnt`, `rep_ratio_tbl`, `common_ratio_tbl`, `inner_col_cnt` 등
    - 이유: 연결도/중복도/구조량 자체가 연속 강도로 해석 가치가 높고, 과도한 구간화 시 정보 손실이 커서 원값 보존이 유리

    3) 제외(feature_table 미포함)
    - 대상: `min_val`, `q1_val`, `avg_val`, `q4_val`, `max_val`, `median_val`, `_tm` 계열 중복 메타 일부, 운영성 메타(`log_id`, `land_no` 등)
    - 이유: 결합 가능성 판단과 직접 연계가 약하거나, 이미 다른 변수로 설명되는 중복 정보이며, 포함 시 패턴 조합 폭발 및 해석 복잡도가 증가

    -> 02_joined_profile_add_variable


    1-3 AOI+Entropy(기준: 0.5 이상)를 적용해 변별력 있는 분석 피처 축을 선별했다(data transformation : /)
    (Entropy 높음 = 분산되어 있음 = 여러 값이 고르게 분포 = 구분 가능, 높은 entropy = 다양한 특성 분포 = 그 특성의 각 카테고리 별로 다른 조인 패턴 찾을 수 있음)

    기준 0.5의 설정이유
    너무 낮추면(예: 0.3) 거의 상수에 가까운 축이 들어와 규칙 수만 늘고 의미가 약해집니다.
    너무 높이면(예: 0.7) 유효 축이 과도하게 줄어 신호를 놓칠 수 있습니다.
    그래서 0.5를 기본선으로 쓴 뒤 필요하면 민감도 비교를 합니다.

    AOI/Entropy에서 중요하게 남은 피처가 cate1, cate2, domain_spread, pk_bucket, dom_sim_bucket, val_sim_bucket, rec_bucket 같은 항목입니다. 

    AOI와 Entropy는 “어떤 축을 보면 조인 가능성이 보이는가”, 즉 “어느 카테고리가 공통 키와 결합축을 많이 갖는가”를 보기 위한 사전 필터링(컬럼 선별) 단계였습니다. (어떤 컬럼을 확인해볼까)

    -> 03_aoi_entropy

    1-4  어떤 공통 컬럼이 반복되는가”를 카테고리별 공통 컬럼 밀도로 확인 ->선별된 9개 피처의 조합만 분석하여 FP-Growth 빈발 패턴 마이닝 진행(빈발 아이템셋 (support >= 5%)최소 5% 이상에서 반복될 때만 의미 있는 빈발패턴으로 본다 )
        트랜잭션 수가 약 9,840이면, 최소 출현 건수는
        9,840        ×        0.05        =        492
        9,840×0.05=492건
        
        너무 낮으면(예: 1~2%)
        우연/노이즈 패턴이 대량으로 들어와 해석이 어려워집니다.
        너무 높으면(예: 10%+)
        의미 있는 중간 빈도 패턴이 탈락해 신호를 놓칩니다.
        5%는
        “노이즈 억제”와 “신호 보존”의 균형점으로 자주 쓰는 실무 기본선입니다.

    1-4-1 교육 카테고리가 데이터 자체가 많은 것 확인
    cate1=교육  →  support 53.2%   ← 전체 9,840행 중 5,232건!
    다른 두 도메인의 2배 이상 많은 데이터가 교육에서 나옴 → 어떤 패턴이든 교육이 섞인 패턴이 상위에 많이 등장

    1-4-2
    교육 내부만 분석한 bi6_L3_freq_교육.csv에서: 교육 도메인 컬럼 = 수치형이고 여러 테이블에 반복 등장하는 패턴이 전체의 절반 이상에서 동일하게 나타남

    domain_spread=대공유(21+)                    70.0%   ← 교육 컬럼은 21+개 테이블에 반복 등장
    col_type=수치형                              57.9%
    col_type=수치형 + domain_spread=대공유(21+) 55.9%   ← 둘이 함께 55.9%!
    rec_bucket=중(101-1k)                       55.0%
    cate2=고등교육                               51.4%


    FP-Growth는 “교육 카테고리가 우연히 많이 보인 게 아니라 반복 조합으로도 강하다”는 걸 확인하는 단계였습니다.
    전체 빈발패턴에서 cate1=교육 자체가 상위에 올라오고, cate1=교육, domain_spread=대공유(21+) 조합도 높은 지지도를 보였습니다.
    이건 “교육 데이터는 단일 파일로 흩어져 있지만, 비슷한 구조와 공통 도메인을 반복적으로 가진다”는 신호입니다.
    다시 말해, 개별 테이블이 따로 놀기보다 같은 언어를 쓰는 테이블 묶음으로 보였다는 뜻입니다.

    빈발패턴은 “교육이 공통 도메인을 반복적으로 가진다”는 걸 확인했고,

        예: "cate1=교육 AND pk_bucket=저PK AND dom_sim_bucket=높은유사"
            같은 패턴이 5% 이상 나타나면 빈발 → 다음 단계로


    1-5 빈발패턴 결과를 보고 연관규칙 확인(연관규칙 (lift >= 1.5 ))

    bi6_L3_rules_교육.csv 상위 규칙:

    "수치형이고 중간 PK인 컬럼이면 → 고등교육 도메인이면서 여러 테이블에 분산되어 있다" 가 무려 95%의 확률이고, 전체 평균 대비 3.76배 더 자주 발생

    IF  col_type=수치형 + pk_bucket=중PK + rec_bucket=중(101-1k)
    THEN cate2=고등교육 + dom_sim_bucket=낮은유사 + domain_spread=대공유(21+)
    confidence: 95.3%,  lift: 3.76

    교육 컬럼의 70.9%가 21개 이상 테이블에 공유되는 도메인으로 나타났습니다. 
    또 교육 카테고리에는 최고 PK 구간에 속하는 컬럼도 많이 분포해 있어 식별축이 비교적 명확했습니다.
    이 단계에서 얻은 결론은:
    교육은 외부와 공유되는 도메인이 많음

    교육은 공통 컬럼이 많이 반복된다
    그중 일부는 식별축으로 쓸 수 있다

    위 분석 결과로 “교육 카테고리 안에서도 학교 단위 공통키를 가진 테이블 묶음”이 유망하다고 판단했습니다.

    따라서 단일 테이블보다는 다중 테이블 결합형 분석이 유리하다
    입니다.



    2. 실제 BI 단계에서는 학교 단위 원천 데이터를 내려받아 마스터 테이블로 확장했습니다. 그 위에서 BI 관점의 인사이트와 규칙을 탐색하는 것을 목표로 설정함.


    2-1 2. 학교명으로 연결가능한 테이블 검색(학교명 단독이 아니라 학교명 정규화 + 시도 + 학교급으로 조인 안정성을 높임.)

    교육 대분류에서 초중등/교육일반 계열만 선택
    학교명 컬럼 보유
    지역 컬럼 보유
    그래서 학교명 정규화 + 시도 정규화 + 학교급 정규화 키로 실제 조인 가능한지 확인 가능한 테이블만 남김

    예)
    원본 표기 A: 조선대학교 부속중학교
    원본 표기 B: 조선대학교부속중학교
    정규화 후 키: (조선대학교부속중학교, 광주, 중)

    2-1실제 공공 데이터맵 검색을 통해 ai/sw학교 등 5~8개의 학교 관련 데이터 다운로드

    411 SW교육 선도학교
    413 학생건강검사
    518 충남학교현황
    540 유초중등교육기관 주소록
    559 AI교육 선도학교
    561 민간참여 학교 컴퓨터교실 운영현황
    599 폐교재산 정보

    2-2 데이터들을 조인하여 마스터 테이블 얻음
    06_school_table_master


    2-3 마스터 테이블에 대해 AOI와 Entropy 분석을 수행하여 설명력이 높은 축과 정보량이 큰 변수를 확인함.


    2-4  분석 목적에 맞게 파생변수를 생성하고, 일부 수치형 변수는 범주화하거나 군집 라벨로 변환하여 재투입함.

    2-5 3~4 단계를 거쳐 정제된 변수와 군집 결과를 포함한 데이터를 기반으로 빈발패턴과 연관규칙을 탐색함.

    2-6 연관규칙 확인결과 AI선도 학교와 타 학교의 연관규칙 탐색함





